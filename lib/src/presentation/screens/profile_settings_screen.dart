import 'package:fittin_v2/src/presentation/screens/profile_hub_screen.dart';

/// Compatibility name retained for routes and tests created before the
/// settings information architecture was split into focused subpages.
class ProfileSettingsScreen extends ProfileHubScreen {
  const ProfileSettingsScreen({super.key});
}
