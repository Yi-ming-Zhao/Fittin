## 1. Audit and responsive layout

- [x] 1.1 Record multi-agent findings for primary pages, pushed pages, state ownership and release risks.
- [x] 1.2 Make Home use width/text-scale-aware compact composition with a vertical overflow escape path.
- [x] 1.3 Reflow Share QR, exercise deep-dive metrics and narrow plan-rule fields at 320 pixels.
- [x] 1.4 Keep Agent history following new output without stealing an intentional user scroll, and remove duplicated keyboard insets.

## 2. Accessibility and theme stability

- [x] 2.1 Give segmented, locale, record-mode and slot selectors selected semantics and at least 44-pixel hit targets.
- [x] 2.2 Give plan editor icon actions localized names and enlarge compact workout actions without changing visual hierarchy.
- [x] 2.3 Add focused large-text, narrow/landscape, selection-semantics and forbidden-color regressions for corrected surfaces.
- [x] 2.4 Verify all five themes offline and either bundle required fonts within the release budget or remove runtime-dependent font resolution.

## 3. State and storage correctness

- [x] 3.1 Bind body metrics state and destructive operations to the active owner, and calculate each current summary from its latest non-null field.
- [x] 3.2 Make Web IndexedDB opening close on version change, fail blocked upgrades within a bounded interval and allow retry.
- [x] 3.3 Commit a workout conclusion atomically and idempotently across log, active instance and local sync queue on native and Web.
- [x] 3.4 Surface preserved synchronization conflicts instead of publishing a successful-sync state.
- [x] 3.5 Serialize Agent submit/retry/resume commands so rapid repeated controls produce one provider run.

## 4. Verification and release

- [x] 4.1 Run focused Flutter data, controller, widget and browser tests plus `git diff --check`.
- [x] 4.2 Run Flutter analyze/test, Android release, iOS no-codesign, Web release, backend Go tests and OpenSpec validation.
- [x] 4.3 Perform bilingual five-theme visual regression at 320×568, 390×844, 390×926 and desktop Web, including large text and keyboard states.
- [x] 4.4 Set `1.2.1+25`, update release notes, open and self-review the PR, and merge only after CI is green.
- [x] 4.5 Tag `v1.2.1`, verify signed CI artifacts/checksums and install the APK over public v1.2.0 without data loss.
- [x] 4.6 Fast-forward the 241 repository, activate the immutable Alibaba Cloud Web release with rollback retained, verify public readiness/Web, and update `latest.json` last.
