## MODIFIED Requirements

### Requirement: Home Screen Prototype Parity
The home screen MUST match the prototype Today layout while using real app state.

#### Scenario: Home screen renders
- **WHEN** the user opens Today
- **THEN** the screen shows the prototype top meta row, session hero, cycle/e1RM stat cards, stepped activity chart, and two quick actions.
- **AND** the visual treatment matches shared theme/primitives without overflow or unrelated colors.
