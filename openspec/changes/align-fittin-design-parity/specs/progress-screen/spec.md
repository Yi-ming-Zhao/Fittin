## MODIFIED Requirements

### Requirement: Progress Screen Prototype Parity
The PR/progress surfaces MUST match the prototype PR dashboard and detail presentation.

#### Scenario: PR dashboard renders
- **WHEN** the user opens Progress
- **THEN** segmented controls, PR cards, lift filters, stepped charts, stats, and milestone rows match the prototype.
- **AND** no old smooth/glowing chart styling remains.
