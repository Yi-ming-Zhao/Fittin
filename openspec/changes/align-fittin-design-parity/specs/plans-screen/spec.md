## MODIFIED Requirements

### Requirement: Plans Screen Prototype Parity
The plans surfaces MUST match the prototype plan library and editor presentation.

#### Scenario: Plan library renders
- **WHEN** the user opens Plans
- **THEN** plan filters, cards, active state, tags, and stat rows match the prototype visual hierarchy.

#### Scenario: Plan editor renders
- **WHEN** the user edits a plan
- **THEN** fields stack safely on narrow widths and day/week selectors use the prototype accent/bordered states.
